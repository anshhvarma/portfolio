import code from '../assets/img/code.png'
import { Parallax } from "react-parallax";

export const Aim = () => {
    return(
        <Parallax strength={1000} bgImage={code} >
        <section className="aim">
        </section>
        <section className='aim'>
        </section>
        </Parallax>
    )
}